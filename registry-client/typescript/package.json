{
  "name": "@learning/registry-client",
  "version": "3.0.0",
  "description": "Self-registration client for the CaaS service catalog (register / heartbeat / deregister).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc -p ."
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  }
}
