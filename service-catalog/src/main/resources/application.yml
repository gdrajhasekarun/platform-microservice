server:
  port: 8761

spring:
  application:
    name: service-catalog

# The catalog is the registry itself — it does NOT self-register with anything.

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always
