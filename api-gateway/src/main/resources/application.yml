server:
  port: 8080

spring:
  application:
    name: api-gateway

catalog:
  url: http://localhost:8761

management:
  endpoints:
    web:
      exposure:
        include: health,gateway
  endpoint:
    health:
      show-details: always
    gateway:
      access: read_only
